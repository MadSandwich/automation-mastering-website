---
import Layout from '@layouts/AuthorisationLayout.astro'
import RegisterAuth from '@components/auth/Register.tsx'

if (Astro.cookies.get('token')) {
	return Astro.redirect('/')
}
---

<Layout title="Register page" description="Register page for automation demo website">
	<RegisterAuth client:load />
</Layout>
